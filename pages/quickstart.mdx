import { Steps } from 'nextra/components'
import { Tabs } from 'nextra/components'

<Steps>
## Step 1 
<Tabs items={['pnpm', 'npm', 'bun']}>
  <Tabs.Tab>
    ```shell copy 
    pnpm i -D dose3ui@latest
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```shell copy 
    npm i -D dose3ui@latest
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```shell copy 
    bun i -D dose3ui@latest
    ```
  </Tabs.Tab>
</Tabs>
 
## Step 2
add plugin config in `tailwind.config.js` file
```js {4} filename="tailwind.config.js" copy
const plugin = require('tailwindcss/plugin')

export default {
  plugins: [require('dose3ui')], // add plugin like this
}
``` 
## Step 3
enjoy with cool layout and components
</Steps>